<div class="jumbotron">
  <div class="container wrapper">
   <div class="col-sm-8">
     <ol class="breadcrumb">
       <li> <%= link_to "songs",songs_path%></li>
     </ol>
     <h2 class="sec_heading"><%= @song.title %></h2>
  </div>

  </div>
</div>
<div class="container content">
  <p id="notice"><%= notice %></p>
  <p>
	  <strong>Name:</strong>
	  <%= @song.title %>
	</p>
  <p>
	  <strong>Album:</strong>
	  <%= @song.album.name %>
	</p>
	<p>
	  <strong>Audio:</strong>
	  <%= @song.audio_file_name %>
	</p>

	<%= link_to 'Edit', edit_song_path(@song) %> |
	<%= link_to 'Back', songs_path %>
</div>
